import styled from "styled-components";
import GlobalStyle from "./GlobalStyle";

const AppBox = styled.div``;

const App = () => {
  return (
    <AppBox>
      <GlobalStyle />
    </AppBox>
  );
};

export default App;
